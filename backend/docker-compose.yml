version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: tienda_postgres
    restart: always
    ports:
    - "5433:5432"

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: tienda
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
